---
name: tailwind-patterns
description: Efficient styling with Tailwind.
---
# Patterns
1. **Colors:** Extend config with `bg-flow-dark` and `text-flow-wine`.
2. **Spacing:** Use consistent padding (e.g., `py-24` for sections).
3. **Gradients:** Use `bg-gradient-to-b` for section fades.
